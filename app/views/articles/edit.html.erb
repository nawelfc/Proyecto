<strong>Editando articulo</strong>
 
<%= form_for :article, url: article_path(@article), html: { multipart: true }, method: :patch do |f| %>
 
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@article.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @article.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :Titulo %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :Texto %><br>
    <%= f.text_area :text, :class => "tinymce", :rows => 10, :cols => 30 %>
    <%= tinymce %>
  </p>
 <%= f.file_field :foto %>
 <br>
  <p>
    <%= f.submit 'Guardar' %>
  </p>
 
<% end %>
 
<%= link_to 'Atras', root_path %>